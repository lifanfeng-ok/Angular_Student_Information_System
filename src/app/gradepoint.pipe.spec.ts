import { GradepointPipe } from './gradepoint.pipe';

describe('GradepointPipe', () => {
  it('create an instance', () => {
    const pipe = new GradepointPipe();
    expect(pipe).toBeTruthy();
  });
});
